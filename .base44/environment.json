{
  "previewPort": 3000,
  "healthPath": "/api/health",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GEMINI_API_KEY",
      "description": "Optional Google Gemini API key for AI assistant, receipt scanning, recipe suggestions, and consumption insights.",
      "requiredAtBoot": false
    }
  ]
}
